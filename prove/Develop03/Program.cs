using System;

class Program
{
    static void Main(string[] args)
    {
        Display test = new Display();
        test.DisplayScripture();
    }
}

// To excede requirements I have the user read the scripture at the start 
// and enter in some sort of hint to help them if needed while memorizing 
// the user then has a choice while memorizing the scripture to look at it
// but it doesn't show by default.